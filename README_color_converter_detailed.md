# Отчёт по лабораторной работе  
## Конвертация цветовых моделей (RGB ↔ HSV ↔ CMYK)

---

## 🎯 Цели работы

Основные цели лабораторной работы:

- Изучить теоретические основы цветовых моделей **RGB**, **HSV**, **CMYK**.  
- Реализовать точные алгоритмы преобразования между ними.
- Создать графическое приложение на **Python (Tkinter)** для работы с цветами.
- Обеспечить **автоматическую синхронизацию** между всеми моделями при изменении любой из них.
- Реализовать обработку ошибок, предупреждений и визуализацию результата.

---

## 📚 Теоретическое исследование цветовых моделей

### 🌈 RGB (Red, Green, Blue)

- Аддитивная модель.
- Используется в мониторах, LED-панелях, цифровой графике.
- Каждая компонента варьируется от **0 до 255**.
- Цвет получается сложением трёх световых потоков.

### 🎨 HSV (Hue, Saturation, Value)

- Модель, удобная для работы с восприятием цвета человеком.
- **H (оттенок)** — угол на цветовом круге (0–360°).  
- **S (насыщенность)** — от 0 до 1.  
- **V (яркость)** — от 0 до 1.  
- Основное назначение: удобно изменять один параметр без влияния на остальные (например, изменить оттенок, сохранив яркость).

### 🖨 CMYK (Cyan, Magenta, Yellow, Key/Black)

- Субтрактивная модель, используется в полиграфии.
- Значения компонентов — от **0 до 1**.
- Цвет формируется при поглощении света (краски уменьшают отражённый свет).

---

## 🔢 Особенности преобразований между моделями

### ✔ RGB → CMYK

Преобразование основано на идее:  
RGB = источник света, CMYK = источник поглощения.

Формулы:

- C = 1 − R/255  
- M = 1 − G/255  
- Y = 1 − B/255  
- K = min(C, M, Y) — общий чёрный компонент  

Затем нормировка для избегания избыточного цвета:

- C = (C − K) / (1 − K)  
- M = (M − K) / (1 − K)  
- Y = (Y − K) / (1 − K)  

Особенность:  
✔ Цвет с малой насыщенностью может иметь большой K.

---

### ✔ CMYK → RGB

Обратное преобразование:

- R = 255 × (1 − C) × (1 − K)  
- G = 255 × (1 − M) × (1 − K)  
- B = 255 × (1 − Y) × (1 − K)  

Особенность:  
✔ Даже небольшое значение K сильно затемняет цвет.

---

### ✔ RGB → HSV

Алгоритм использует максимум и минимум среди R, G, B:

- V = max(R, G, B) / 255  
- S = (V - min) / V  
- H вычисляется по тому, какая компонента максимальная.

Особенности:
- При diff = 0 оттенок неопределён — принимаем H = 0.
- HSV удобно для "поворота" цвета по кругу.

---

### ✔ HSV → RGB

Используются 6 секторов по 60°.

- Вычисляется вспомогательная величина С = V×S.
- X = C × (1 − |(H/60) mod 2 − 1|).
- Затем один из шести вариантов распределения (C,X,0).
- После этого доводится до диапазона 0–255 через `R=(R+C)*255`.

Особенность:
✔ Яркость и насыщенность влияют линейно, что удобно при создании UI‑инструментов.

---

## 🔧 Реализация алгоритмов в проекте

В проекте включены функции:

- `rgb_to_cmyk(r, g, b)`
- `cmyk_to_rgb(c, m, y, k)`
- `rgb_to_hsv(r, g, b)`
- `hsv_to_rgb(h, s, v)`

Каждая функция:
- Является чистой
- Не зависит от интерфейса
- Выполняет строго математические преобразования

---

## 🖥 Интерфейс приложения

Приложение построено на **Tkinter** и содержит:

### ✳ Блок RGB
- Ползунки R, G, B (0–255)
- Поля ввода
- События:
  - Изменение по Enter
  - Изменение при перетаскивании ползунка

### ✳ Блок HSV
- Ползунки для H (0–360), S и V (0–1)
- Поля ввода
- Автоматический пересчёт в RGB и CMYK

### ✳ Блок CMYK
- Ползунки C, M, Y, K (0–1)
- Поля ввода

### 🎨 Визуализатор цвета
- Отображает живой результат
- Цвет обновляется при любом изменении

### 🎛 Кнопка «Выбрать цвет»
- Открывает стандартный colorpicker Tkinter
- Обновляет все модели

---

## 🔁 Механизм автоматической синхронизации

Во избежание рекурсивных вызовов использован флаг:

```
self.updating = True/False
```

Пример работы:

1. Пользователь двигает ползунок HSV.
2. Метод `update_from_hsv()`:
   - устанавливает `self.updating = True`
   - пересчитывает RGB
   - пересчитывает CMYK
   - обновляет интерфейс
   - сбрасывает флаг

3. Пока флаг активен:
   - другие обработчики **игнорируют события**
   - предотвращается “зацикливание”, напр.:
     HSV → RGB → HSV → RGB → …

Особенности синхронизации:
- Все значения округляются с учётом диапазонов
- Пересчёт происходит плавно
- Исключён дребезг событий при движении ползунка

---

## ⚠ Обработка некорректных значений

- Ввод ограничивается min/max.
- Любое значение приводится к допустимому.
- Если преобразование даёт выход за пределы RGB (0–255), появляется предупреждение:

```
"RGB усечён!"
```

---

## 🧪 Тестирование

Проведены тесты:

| Тип теста | Пример | Результат |
|----------|---------|-----------|
| Чистые цвета | (255,0,0), (0,255,0), (0,0,255) | Корректно |
| Граничные значения | белый, чёрный, серый | Без ошибок |
| Насыщенные цвета | фиолетовый, ярко-зелёный | Без искажений |
| Цвета из colorpicker | любые случайные | Полная синхронизация |

---

## Итоговый вывод

В ходе работы было разработал полноценное приложение для конвертации
цветов между моделями RGB, HSV и CMYK, включающее точные алгоритмы
преобразования и удобный графический интерфейс. Решил проблему с выходом
за границы для RGB, путем вывода уведомления. Была проблема с переводом 
с одних цветовых моделей в другие, проблема была в библиотеке, переписал сам и все
заработало. Реализовал автооматическую синхронизация всех цветовых представлений, что
обеспечивает корректность и плавность обновлений. Итоговая программа
полностью отвечает поставленным требованиям и может служить практическим
инструментом для работы с цветовыми моделями.


